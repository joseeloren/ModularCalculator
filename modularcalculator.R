n <- readline(prompt="This is a modular calculator. Choose an option: 1. Add 2. Subtract 3. Multiply 4. Divide")
n1 <- readline(promt="First num")
n2 <- readline(promt="Second num")
nI = as.integer(n)

if (nI == 1) {
  n1+n2;
}
